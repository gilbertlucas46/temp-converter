import styled from 'styled-components';

const DividerStyles = styled.div`
    svg {
        position: absolute;
        bottom: 0;
        top: 0;
        margin: auto;
        z-index: 9;
    }
`;

export default DividerStyles;
